#include "main.h"
/**
 * mul - multiplies 2 integers
 * @a: first integer
 * @b: second integer
 * Return: 0
 */
int mul(int a, int b)
{
	int i = 0;
	char k;

	k[] = a * b;

	while (k[i] != '\0')
	{
		_putchar(k[i]);
		i++;
	}
	return (0);
}
