#include  "main.h"
/**
 * _abs - prints absolute value of a number
 * @n: number to be checked
 * Return: absolute value of number
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (n * -1);
}
