#include <stdio.h>
/**
 * main - entry block
 * Return: 0
 */
int main (void)
{
	int a = 0, b = 1, n = 2, d;

	printf("%i, %i", a, b);

	while (n < 50)
	{
		a + b = d;
		printf(", %i", d);
		a = b;
		b = d;
		n++;
	}
	printf("\n");
	return (0);
}
