#include <stdio.h>
/**
 * main - prints program name
 * @argc: argument count
 * @argv: array of arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
